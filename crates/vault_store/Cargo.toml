[package]
name = "vault_store"
version = "0.1.0"
edition = "2021"

[dependencies]
crypto_core = { path = "../crypto_core" }
directories = "5.0"
thiserror = "1.0"
vault_domain = { path = "../vault_domain" }
vault_format = { path = "../vault_format" }

[dev-dependencies]
tempfile = "3.10"
